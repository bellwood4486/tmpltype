# サポートされるテンプレート構文

> **📖 英語版の詳細ドキュメント:** [Template Syntax (English)](../template-syntax.md)

`tmpltype`は幅広いGoテンプレート構文パターンをサポートしています。テンプレートスキャナがこれらのパターンを解析して、テンプレートパラメータの型を自動的に推論します。

## 目次

- [基本パターン](#基本パターン)
- [制御フロー](#制御フロー)
- [コレクション](#コレクション)
- [高度なパターン](#高度なパターン)
- [型推論ルール](#型推論ルール)
- [制限事項](#制限事項)
- [サンプル](#サンプル)

## 基本パターン

### 1. フィールド参照

```go
{{ .Title }}
```

生成される構造体に`string`フィールドを作成します。

### 2. ネストされたフィールド参照

```go
{{ .User.Name }}
{{ .Author.Email }}
```

`string`フィールドを持つネストされた構造体型を作成します。

### 3. 深くネストされたパス

```go
{{ .Company.Department.Team.Manager.Name }}
```

完全なパスに従って深くネストされた構造体型を作成します。

## 制御フロー

### 4. 条件文（if）

```go
{{ if .Status }}
  <p>ステータス: {{ .Status }}</p>
{{ end }}
```

条件内のフィールドは、子フィールドがある場合は構造体として、それ以外は`string`として推論されます。

### 5. with文

```go
{{ with .Summary }}
  <p>{{ .Content }}</p>
{{ end }}
```

ブロック内のドット（`.`）コンテキストを変更します。スキャナはスコープの変更を正しく追跡します。

### 6. else句

```go
{{ with .Summary }}
  <p>{{ .Content }}</p>
{{ else }}
  <p>{{ .DefaultMessage }}</p>
{{ end }}
```

`else`ブロック内のフィールドは親スコープにあります。

## コレクション

### 7. スライスのrange

```go
{{ range .Items }}
  <li>{{ .Title }} - {{ .ID }}</li>
{{ end }}
```

`.Items`をrangeの本体内のフィールドを持つスライス型`[]struct{...}`として推論します。

### 8. index関数によるマップアクセス

```go
{{ index .Meta "key" }}
{{ index .Meta "author" }}
```

`index`関数を使用する場合、`.Meta`を`map[string]string`として推論します。

## 高度なパターン

### 10. ネストされた構造（with + range）

```go
{{ with .Project }}
  <h3>{{ .Name }}</h3>
  {{ range .Tasks }}
    <p>{{ .Title }} - {{ .Status }}</p>
  {{ end }}
{{ end }}
```

`with`と`range`を組み合わせて、スライスフィールドを持つネストされた構造体階層を作成します。

### 11. 複数のrange

```go
{{ range .Authors }}
  <h4>{{ .Name }}</h4>
{{ end }}

{{ range .Tags }}
  <span>{{ . }}</span>
{{ end }}
```

- `{{ . }}`はrangeで`[]string`（シンプルな値のスライス）を作成
- `{{ .Field }}`はrangeで`[]struct{Field string}`を作成

## 型推論ルール

### デフォルトの型推論

| パターン | 推論される型 | 例 |
|---------|---------------|---------|
| `{{ .Field }}` | `string` | `Title string` |
| `{{ .Obj.Field }}` | `string`を持つネストされた構造体 | `User struct{Name string}` |
| `{{ range .Items }}...{{ end }}` | `[]struct{...}` | `Items []struct{...}` |
| `{{ index .Map "key" }}` | `map[string]string` | `Meta map[string]string` |

### フィールド命名

生成される構造体フィールド：
- **アルファベット順**にソートされます
- **エクスポート**名（大文字）を使用します
- Go命名規則に従います

**テンプレート:**
```go
{{ .user_name }}
{{ .user-email }}
{{ .UserID }}
```

**生成:**
```go
type TemplateParams struct {
    UserEmail string  // user-emailから
    UserID    string  // UserIDから
    UserName  string  // user_nameから（アルファベット順）
}
```

## 制限事項

### サポートされていない

❌ **変数（代入）**
```go
// 型推論では解析されない
{{ $var := .Something }}
{{ $var.Field }}
```

**回避策:** 変数を使わずに直接フィールドにアクセスするか、`@param`を使用

❌ **フィールドに対する関数呼び出し**
```go
// 関数呼び出しは解析されない
{{ .Title | upper }}
{{ printf "%s" .Name }}
```

**回避策:** 関数で使用されるフィールドは推論されますが、関数の結果は推論されません

## サンプル

### 完全な例

すべてのサポートされるパターンを示す動作例については、[サンプル04: Comprehensive Template](../../examples/04_comprehensive_template/)を参照してください。

**テンプレートファイル:**
- `basic_fields.tmpl` - 基本とネストされたフィールド参照
- `control_flow.tmpl` - If、with、else文
- `collections.tmpl` - Rangeとマップアクセス
- `advanced.tmpl` - 複雑なネストされた構造

### ユースケース別クイックリファレンス

| ユースケース | テンプレートパターン | サンプル |
|----------|------------------|-------------|
| シンプルなデータ | `{{ .Field }}` | [01_basic](../../examples/01_basic/) |
| ネストされたオブジェクト | `{{ .User.Name }}` | [01_basic](../../examples/01_basic/) |
| リスト | `{{ range .Items }}{{ .Title }}{{ end }}` | [04_comprehensive](../../examples/04_comprehensive_template/) |
| マップ | `{{ index .Meta "key" }}` | [04_comprehensive](../../examples/04_comprehensive_template/) |
| 条件 | `{{ if .Flag }}...{{ end }}` | [04_comprehensive](../../examples/04_comprehensive_template/) |
| 型の上書き | `{{/* @param Age int */}}` | [02_param_directive](../../examples/02_param_directive/) |
| 複雑な型 | `{{/* @param Items []struct{...} */}}` | [05_all_param_types](../../examples/05_all_param_types/) |

## 関連項目

- [はじめに](getting-started.md) - チュートリアルと基本
- [`@param`ディレクティブ](param-directive.md) - 推論された型の上書き
- [サンプル](../../examples/) - 動作するコード例
- [English version (詳細)](../template-syntax.md) - Complete reference in English
