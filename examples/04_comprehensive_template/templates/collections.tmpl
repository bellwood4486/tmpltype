<!DOCTYPE html>
<html>
<head>
    <title>Collections - Template Features</title>
</head>
<body>
    <h1>Working with Collections</h1>

    {{/* 5. Range over slice */}}
    <section id="range">
        <h2>Range Over Slice</h2>
        <ul class="items">
        {{ range .Items }}
            <li>
                <strong>{{ .Title }}</strong>
                <span>ID: {{ .ID }}</span>
                <p>{{ .Description }}</p>
            </li>
        {{ end }}
        </ul>
    </section>

    {{/* 6. Index function for map access */}}
    <section id="map">
        <h2>Map Access with Index Function</h2>
        <dl class="metadata">
            <dt>Environment</dt>
            <dd>{{ index .Meta "env" }}</dd>
            <dt>Version</dt>
            <dd>{{ index .Meta "version" }}</dd>
            <dt>Build</dt>
            <dd>{{ index .Meta "build" }}</dd>
        </dl>
    </section>

    {{/* 7. Range over map with struct values */}}
    <section id="map-struct">
        <h2>Map with Struct Values</h2>
        <div class="users">
        {{ range $key, $value := .Users }}
            <article class="user">
                <h3>{{ $key }}</h3>
                <p>Name: {{ .Name }}</p>
                <p>Email: {{ .Email }}</p>
                <p>Role: {{ .Role }}</p>
            </article>
        {{ end }}
        </div>
    </section>

    <footer>
        <p>Generated by tmpltype - Collections Template</p>
    </footer>
</body>
</html>
